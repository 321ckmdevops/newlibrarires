def sum(a,b)
{
  ecbho "The sum of ${a} and ${b} is ${a+b}"
}
def sub(a,b)
{
  ecbho "The substraction of ${a} and ${b} is ${a-b}"
}
def mul(a,b)
{
  ecbho "The multipcation of ${a} and ${b} is ${a*b}"
}

def dev(a,b)
{
  ecbho "The devision of ${a} and ${b} is ${a/b}"
}
