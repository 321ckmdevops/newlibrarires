def sum(a,b)
{
  echo "The sum of ${a} and ${b} is ${a+b}"
}
def sub(a,b)
{
  echo "The substraction of ${a} and ${b} is ${a-b}"
}
def mul(a,b)
{
  echo "The multipcation of ${a} and ${b} is ${a*b}"
}

def dev(a,b)
{
  echo "The devision of ${a} and ${b} is ${a/b}"
}
